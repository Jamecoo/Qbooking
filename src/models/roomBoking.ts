export interface RoomModel {
    _id: string;
    images: string;
    roomName: string;
    is_active_status: boolean;
    floor: string;
    type_room: string;
    description: string;
    is_active_booked: boolean

}